School Management API

Backend-сервіс для управління навчальними групами, студентами та інтеграцією з зовнішніми курсами.
🏗 Опис структури проекту

Проект побудований на архітектурі NestJS із чітким розділенням відповідальності:

    src/groups/ — модуль управління групами (логіка агрегації активних груп, додавання студентів через транзакції).

    src/students/ — модуль управління студентами (валідація унікальності email, створення профілів).

    src/external/ — модуль інтеграції із зовнішнім API (проксі-логіка, кешування, трансформація даних).

    src/common/ — глобальні фільтри помилок та утиліти.

    test/ — E2E тести для перевірки цілісності роботи API та бази даних.

🔌 Реалізація інтеграції та кешування

Для отримання даних про курси використовується зовнішній сервіс fakestoreapi.com.
Інтеграція:

    Використовується Axios із налаштованим timeout: 3000ms.

    Реалізовано трансформацію даних (Data Mapping), що дозволяє віддавати клієнту лише необхідні поля (id, title, price), приховуючи зайву інформацію від API.

Кешування:

    Реалізовано In-memory cache із TTL (Time To Live) 5 хвилин.

    Resilience (Відмовостійкість): Якщо зовнішній API недоступний (Timeout або Error), система автоматично перевіряє наявність даних у кеші. Якщо кеш існує (навіть якщо термін TTL добіг кінця, але дані ще в пам'яті), сервіс поверне старі дані замість помилки.

    Якщо API лежить і кеш порожній — повертається статус 503 Service Unavailable.

🛠 Обґрунтування вибору фреймворку

Для реалізації обрано NestJS з наступних причин:

    Модульність: Дозволяє легко масштабувати проект, розділяючи логіку на незалежні блоки (Groups, Students, External).

    Dependency Injection: Потужна система впровадження залежностей спрощує тестування (mocking) та підтримку коду.

    TypeScript: Найкраща підтримка типізації "з коробки", що мінімізує помилки на етапі розробки.

    Стандартизація: NestJS задає чітку структуру коду, що важливо для командної роботи та подальшої підтримки.

🚀 Інструкція запуску

Найпростіший спосіб запустити проект — через Docker Compose. Це автоматично підніме базу даних PostgreSQL та API сервіс.
Попередні вимоги:

    Встановлений Docker та Docker Compose.

Кроки:

    Клонуйте репозиторій.

    Запустіть контейнери:
    Bash

    ```bash 
    docker-compose up --build
    ```
    Результат:

        API буде доступне за адресою: http://localhost:3000

        База даних PostgreSQL на порту: 5432

    Важливо: При старті контейнера school_api автоматично запустяться E2E тести. Додаток почне роботу лише після їх успішного проходження.

🧪 Тестування

Для перевірки критичної бізнес-логіки реалізовано:

    Unit-тест для GroupsService (перевірка фільтрації активних груп через SQL-агрегацію).

    Unit-тест для ExternalService (перевірка кешування та поведінки при падінні API).

    E2E-тести для основних ендпоінтів (POST /students, GET /groups/active, GET /external/courses).

Запуск тестів вручну:
Bash

# Unit-тести
```bash
npm run test
```

# E2E-тести
```bash
npm run test:e2e
```
